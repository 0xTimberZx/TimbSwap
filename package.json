{
  "name": "timbswap-frontend-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only tooling for the static TimbSwap frontend. The site itself has no build step (GitHub Pages serves the repo as-is); this package only produces the vendored bundles committed under vendor/.",
  "scripts": {
    "build:vendor": "node scripts/build-vendor.mjs"
  },
  "devDependencies": {
    "@hpke/chacha20poly1305": "1.8.0",
    "@hpke/core": "1.9.0",
    "@privy-io/js-sdk-core": "0.76.0",
    "@simplewebauthn/browser": "14.0.0",
    "esbuild": "^0.25.0",
    "qrcode-generator": "2.0.4"
  }
}
